# todolist-os-termpaper

## Installation

To develop in local environment, installing dependencies by
`poetry install`
then
`poetry shell`

Add any additional packages you need by
`poetry add [named package]`
then
`poetry lock`
